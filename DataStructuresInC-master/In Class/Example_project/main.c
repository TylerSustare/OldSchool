#include <stdio.h>
#include <stdlib.h>

int main() // all sudo code today
{
   *temp // pretend it was malloc and declared
   if (d->head == NULL)
      temp->value = val
      temp->next = tail
      temp->prev = head
      head->next = temp
      tail->prev = temp
      else
         temp->next = head->next
         temp->prev = prev
         head->next->prev = temp
         head->next = temp


      popStack(){// delete from the front of a linked list
       assert(head->next != NULL)
       *dead = head->next
       head->next = head->next->next
       head->next->prev = head
       dead->next = 0
       dead->prev = 0
       free(dead)


      }
   return 0;
}
